<?
  /* 
   * We check this var in index.php file, to start the install script at the first run of core 
   *
   * @param: is true if core havn't installed before 
   *         is false if Core is succesfully installed
   */
  
		$installerActive = true; 
?>